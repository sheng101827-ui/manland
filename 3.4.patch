diff --git a/src/main/java/com/example/sens/service/OrderService.java b/src/main/java/com/example/sens/service/OrderService.java
index 58d5ad2..bc0a69e 100644
--- a/src/main/java/com/example/sens/service/OrderService.java
+++ b/src/main/java/com/example/sens/service/OrderService.java
@@ -35,4 +35,11 @@ public interface OrderService extends BaseService<Order, Long> {
      */
     Order findByPostId(Long postId);
 
+    /**
+     * 退租并退还押金
+     * @param orderId
+     * @return
+     */
+    String refund(Long orderId);
+
 }
diff --git a/src/main/java/com/example/sens/service/impl/OrderServiceImpl.java b/src/main/java/com/example/sens/service/impl/OrderServiceImpl.java
index 912ba9d..26aff3d 100644
--- a/src/main/java/com/example/sens/service/impl/OrderServiceImpl.java
+++ b/src/main/java/com/example/sens/service/impl/OrderServiceImpl.java
@@ -4,21 +4,35 @@ import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
 import com.baomidou.mybatisplus.core.mapper.BaseMapper;
 import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
 import com.example.sens.entity.Order;
+import com.example.sens.entity.Post;
+import com.example.sens.entity.User;
+import com.example.sens.enums.OrderStatusEnum;
 import com.example.sens.mapper.OrderMapper;
 import com.example.sens.service.OrderService;
+import com.example.sens.service.PostService;
+import com.example.sens.service.UserService;
+import lombok.extern.slf4j.Slf4j;
 import org.springframework.beans.factory.annotation.Autowired;
 import org.springframework.stereotype.Service;
+import org.springframework.transaction.annotation.Transactional;
 
 /**
  * @author 言曌
  * @date 2020/4/6 2:01 下午
  */
 @Service
+@Slf4j
 public class OrderServiceImpl implements OrderService {
 
     @Autowired
     private OrderMapper orderMapper;
 
+    @Autowired
+    private UserService userService;
+
+    @Autowired
+    private PostService postService;
+
     @Override
     public BaseMapper<Order> getRepository() {
         return orderMapper;
@@ -65,4 +79,36 @@ public class OrderServiceImpl implements OrderService {
     public Order findByPostId(Long postId) {
         return orderMapper.findByPostId(postId);
     }
+
+    @Override
+    @Transactional(rollbackFor = Exception.class)
+    public String refund(Long orderId) {
+        Order order = get(orderId);
+        if (order == null) {
+            return "订单不存在";
+        }
+
+        Post post = postService.get(order.getPostId());
+        Long deposit = post.getDeposit();
+
+        User ownerUser = userService.get(order.getOwnerUserId());
+        User tenantUser = userService.get(order.getUserId());
+
+        order.setStatus(OrderStatusEnum.FINISHED.getCode());
+        update(order);
+
+        ownerUser.setMoney(ownerUser.getMoney() - deposit);
+        userService.update(ownerUser);
+
+        try {
+            tenantUser.setMoney(tenantUser.getMoney() + deposit);
+            userService.update(tenantUser);
+        } catch (Exception e) {
+            log.error("给租客退还押金时发生异常，订单ID：{}，异常信息：{}", orderId, e.getMessage(), e);
+            order.setStatus(OrderStatusEnum.DEPOSIT_RETURN_FAIL.getCode());
+            update(order);
+        }
+
+        return "退租成功";
+    }
 }
